{
  "name": "a2l-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["src-ui"],
  "scripts": {
    "dev": "npm run dev --workspace=src-ui",
    "build": "npm run build --workspace=src-ui",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "test:browser": "cd src-ui && vite --config vite.test.config.ts",
    "test:browser:preview": "cd src-ui && vite build --config vite.test.config.ts && vite preview --port 5174"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
